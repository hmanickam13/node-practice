
console.log(); // global

setTimeout() // delay function call
clearTimeout()

setInterval() // repeatedly call function after a delay
clearInterval() // stop the function from being called repeatedly

window.console.log // in browsers we can access all the global files
window.setTimeout()
// js engine will prescript "window."


var message = '';
window.message

// we use global in node instead of window
global.console.log
global.setInterval()

// the variables like 'message' are not added to the global object
// message has scope only for this file


